// No-op stub for the `server-only` guard so the eval harness can import the
// parser under plain `tsx` (outside Next's bundler). Aliased ONLY for the eval
// via eval/tsconfig.json — the app build still uses the real package.
export {};
